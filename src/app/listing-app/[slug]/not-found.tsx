import { ErrorSection } from '@/components/shared/ErrorSection';

export default function NotFound() {
  return (
    <ErrorSection message="Sorry! The listing you are looking for is not available." />
  );
}
